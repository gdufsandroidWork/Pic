<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".FindPasswordActivity" >

    <RelativeLayout
        android:id="@+id/relativeLayout1"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dip"
        android:background="#55FFFFFF"
        android:padding="10dip" >

        <TextView
            android:id="@+id/register_findpassword_textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/email_input_string"
            android:textSize="15sp" />

        <EditText
            android:id="@+id/register_findpassword_editView"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_alignLeft="@id/register_findpassword_textView"
            android:layout_below="@+id/register_findpassword_textView"
            android:layout_marginTop="5dp"
            android:text="@string/email_hit_string"
            android:textSize="15sp" />

        <ImageView
            android:id="@+id/Ver_Code"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_below="@id/register_findpassword_editView"
            android:src="@drawable/ic_launcher" />
    </RelativeLayout>

</RelativeLayout>